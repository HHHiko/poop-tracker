# 开发日志 — 2026-06-03

## 当前阶段
阶段 6：打磨与联调（进行中）

## 今日完成
- [x] 详情页增加「添加便便 / 经期记录」按钮
  - detail.wxml: 顶部栏下方新增醒目按钮（漫画风棕底白字）
  - detail.js: onAddRecord() 跳转 record tab（先 switchTab+全局变量，后改为 reLaunch）
  - detail.wxss: 新增加按钮样式
  - app.js: 新增/清理 pendingRecordDate 字段
  - record.js: 新增/简化 onShow()
- [x] 详情页经期快速切换 — 一键标记/取消，无需跳转
  - detail.wxml: 粉色经期切换按钮（已标记/未标记双态）
  - detail.js: onPeriodToggle() 即时切换 + 通知日历刷新
  - detail.wxss: 经期按钮漫画风双态样式
  - onShow 补充刷新 isPeriod 状态
- [x] 经期预测方案设计（plan 文件已更新，待实施）
- [x] 经期预测功能实现
  - storage.js: predictPeriod() — 连续日期分组 → 统计周期长度/持续天数 → 预测下次
  - calendar.js: _renderCalendar 调用预测，生成 predictedSet，单元增加 isPredicted 字段
  - calendar.wxml: 预测日期 class="period-predicted" + 虚线横条 + 图例条目
  - calendar.wxss: 浅粉底色 + 渐变虚线横条 + 图例虚线方块

- [x] 详情页删除按钮修复 — 小程序版 + Web 版同步
  - detail.wxml: 替换「长按删除」为可见 🗑 按钮（catchtap 防冒泡）
  - detail.wxss: 红色漫画风按钮 64rpx，带偏移阴影 + 按下态
  - web/js/app.js: 替换 delete-hint span 为 delete-btn div（stopPropagation）
  - web/css/style.css: 新增 .delete-btn 样式 36px 红色按钮
  - 保留 bindlongpress 作为备选删除方式

## 待办事项
- [x] 实现经期预测功能（storage.predictPeriod + 日历标记）
- [x] PWA 版本转换：小程序 → 网页应用
- [ ] 测试 PWA 版本在 iOS Safari 上的效果
- [ ] 部署到静态托管平台
- [ ] 微信开发者工具中预览全部效果
- [ ] 真机预览测试

## 遇到的问题
- record 页是 tabBar 页面，最初用 wx.switchTab + 全局变量传递日期，但存在时序问题导致日期无法正确传递
- 已修复：改用 wx.reLaunch 直接传 query 参数到 record 页的 onLoad，同时清理了全局变量和 onShow 的冗余逻辑
- PWA 转换中 storage.js 命名空间问题：小程序用 require 模块，PWA 用全局作用域，通过末尾添加 storage 对象包装解决

## PWA 转换记录（2026-06-03）
- [x] web/js/storage.js — localStorage 版数据层（API 与小程序版一致）
- [x] web/js/app.js — SPA 主控制器（三视图路由 + Toast + Modal）
- [x] web/css/style.css — 合并全部样式，rpx→px 转换
- [x] web/index.html — 单页 HTML（三视图 + 底部 Tab + 导出弹窗）
- [x] web/manifest.json — PWA 清单（SVG 内嵌图标，免额外文件）
- [x] web/sw.js — Service Worker（离线缓存）

## 下次计划
- 本地测试 PWA 版本
- 部署到 GitHub Pages / Netlify 等免费平台
